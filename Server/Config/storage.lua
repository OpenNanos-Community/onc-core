-- Due to the sensibility of the data, the variable is local.
local tSqlCredentials = {
    databaseType = "SQLite", -- SQLite / MySQL / PostgreSQL (write the name of the database type you are using exacly as it is written here)
    host = "",
    user = "",
    password = "",
    database = "",
    port = 3306
}

